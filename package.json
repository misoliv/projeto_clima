{
  "name": "projeto_clima",
  "version": "1.0.0",
  "description": "Aplicativo simples de previsão do tempo usando Open-Meteo API",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0"
  },
  "jest": {
    "testMatch": ["<rootDir>/assets/**/*.test.js"],
    "testEnvironment": "jest-environment-jsdom"
  }
}
